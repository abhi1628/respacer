# Core (required) -- enough to fix .docx files
python-docx>=1.1.0
wordninja>=2.0.0

# Optional -- only needed for PDF input support (pip install respacer[pdf])
# pdfplumber>=0.11.0

# Optional -- only needed for LLM-enhanced mode (pip install respacer[llm])
# requests>=2.31.0
